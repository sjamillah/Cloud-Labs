AWSTemplateFormatVersion: '2010-09-09'
Description: Private ECR repository for lab1-ecr-pipeline with security best practices
 
Parameters:
  RepositoryName:
    Type: String
    Default: flask-demo-app
  CIRoleArn:
    Type: String
    Description: ARN of the GitHub Actions OIDC role allowed to push images
    Default: arn:aws:iam::697863031884:role/ecr-github-lab-role
 
Resources:
  AppRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref RepositoryName
      ImageTagMutability: MUTABLE
      ImageScanningConfiguration:
        ScanOnPush: true
      EncryptionConfiguration:
        EncryptionType: AES256 
      RepositoryPolicyText:
        Version: '2012-10-17'
        Statement:
          - Sid: AllowPushFromCIRoleOnly
            Effect: Allow
            Principal:
              AWS: !Ref CIRoleArn
            Action:
              - ecr:BatchCheckLayerAvailability
              - ecr:PutImage
              - ecr:InitiateLayerUpload
              - ecr:UploadLayerPart
              - ecr:CompleteLayerUpload
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Expire untagged images older than 14 days",
                "selection": {
                  "tagStatus": "untagged",
                  "countType": "sinceImagePushed",
                  "countUnit": "days",
                  "countNumber": 14
                },
                "action": { "type": "expire" }
              },
              {
                "rulePriority": 2,
                "description": "Keep only the last 10 tagged images",
                "selection": {
                  "tagStatus": "tagged",
                  "tagPrefixList": ["<FULL_NAME>_ecr-github-app"],
                  "countType": "imageCountMoreThan",
                  "countNumber": 10
                },
                "action": { "type": "expire" }
              }
            ]
          }
      Tags:
        - Key: Project
          Value: lab1-ecr-docker-pipeline
        - Key: Environment
          Value: lab
        - Key: ManagedBy
          Value: CloudFormation
 
Outputs:
  RepositoryUri:
    Value: !GetAtt AppRepository.RepositoryUri
